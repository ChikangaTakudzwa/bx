apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bx-pvc
spec:
  storageClassName: skills-network-learner
  resources:
    requests:
      storage:  1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany